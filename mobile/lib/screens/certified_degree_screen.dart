import 'package:flutter/material.dart';

class CertifiedDegreeScreen extends StatelessWidget {
  const CertifiedDegreeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Certified Degrees')),
      body: const Center(child: Text('Certified Degrees Coming Soon')),
    );
  }
}
